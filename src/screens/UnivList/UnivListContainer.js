import React, { useState } from 'react';
import UnivListPresenter from './UnivListPresenter';

const UnivListContainer = (props) => {
    const { } = props;

    return (
        <UnivListPresenter />
    );
}

export default UnivListContainer;